/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.env
.env*
!.env.example
.DS_Store
npm-debug.log*

# Generated fighter photos (bg-removed) + work files
/public/headshots/
scripts/.photowork/
og-preview.png

# RC-1 / Playwright E2E + Lighthouse outputs (evidence, not source)
/test/e2e/.artifacts/
/test/e2e/.auth/
/test/e2e/report/
/test/e2e/screenshots/
/test/e2e/results.json
/test/e2e/chromium-results.json
/test/lighthouse/reports/
/playwright-report/
/test-results/

# cleanup rollback exports (contain fight rows — never commit)
wikicard-cleanup-backup-*.json

# Locally generated fighter image variants (sharp -> public/). 3,402 files / 84MB
# from a storage-unconfigured run. The real copies live in R2; these are a local
# artefact of the fallback path and must never be committed.
/public/fighters/
/public/headshots/

# Private evidence / identity documents stored on local disk in dev.
# NEVER tracked: in local development this directory holds real uploads.
.private/
